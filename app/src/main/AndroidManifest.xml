<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayoub.dosecal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DOSECAL">
        <activity android:name=".add_regle"></activity>
        <activity android:name=".update_pat" />
        <activity android:name=".add_med" />
        <activity android:name=".add_pat" />
        <activity
            android:name=".Calculator"
            android:parentActivityName=".Home" />
        <activity
            android:name=".About"
            android:parentActivityName=".Calculator" />
        <activity
            android:name=".Medicament"
            android:parentActivityName=".Home" />
        <activity
            android:name=".Patient"
            android:parentActivityName=".Home" />
        <activity
            android:name=".Home"
            android:parentActivityName=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>